<form method="post" action="{{ path('app_admin_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette marque ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
